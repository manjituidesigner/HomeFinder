# Rently Backend

Minimal Express API for development.

Run:

```bash
cd Rently/backend
npm install
npm start
```

Endpoints:

- `GET /api/health` — health check
- `GET /api/properties` — sample properties
